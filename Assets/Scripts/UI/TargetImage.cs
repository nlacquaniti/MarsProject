using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class TargetImage : MonoBehaviour
{
    /*
    public Image image;

    private UIManager m_Manager;

    
    private void Awake()
    {
        m_Manager = GameObject.Find("GameManager").GetComponent<UIManager>();
    }

    void Start ()
    {
        m_Manager.GetMapImages(this.gameObject, image);	
	}

    private void OnDestroy()
    {
        m_Manager.RemoveMapImage(this.gameObject);
    }
    */

}
